# Virtual Machine?
## Types of Virtual Machines?
- Hardware based VM
- Application based VM
### Types of hardware based VM?
KVM, Kernel VM and Xen
# Basic architecture diagram of JVM?
# Class Loader Sub-System?
- Loading
- Linking
- Initialization
## How many types of class loaders are there in Java?
- Bootstrap class loader
- Extension class loader
- Application class loader
## How Java class loaders works?
 Based on algorithm, Deligation hierarchy algorithm.
## Need of customized class loader?
Where default class loading is not suitable for oue requirement how we can define our own
customize class loaders?
## Pseudocode to define customized class loader?
Class loader class present inside java.lang package to customize our own class loader.

## Various memory areas of JVM?
- Method Area -- Per JVM
- Heap Area -- Per JVM
- Stack Area -- Per thread
- PC Registers -- Per thread
- Native method stacks -- Per thread

## Program to display heap memory statistics?
- maxMemory()
- totalMemory()
- freeMemory()
These above methods are called on run time object

## How to set maximum and minimum heap sizes?

## Execution Engine?
- Interpreter 
- JIT Compiler

## Java Native Interface (JNI)?
## Complete Architecture Diagram of JVM?
## Class File Structure?
--------------------In depth(need to finish)-----------------------